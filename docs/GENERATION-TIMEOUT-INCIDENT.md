# 生成超时误报与重复提交

> 维护说明（2026-09-15）：当前能力、入口与限制见 [当前实现状态](CURRENT-STATUS.md)。本文中的版本验收数字、当时状态和后续计划保留为历史记录；涉及 RunningHub 的接入计划已取消，相关接口现已移除。

## 已确认的证据

- 本地任务 `1d6e02e6-57de-4022-abe4-655eed8f6c56` 记录 `The operation was aborted due to timeout`，旧状态为 failed。
- 原生成服务对提交请求等待 120 秒；用户提供的上游记录显示一次图片请求耗时 156 秒。
- 后续任务 `47cb934a-bf61-4cae-bf86-9202ae55bbd3` 已 completed，后端有实际图片 URL。

客户端将提交超时当作明确生成失败，允许用户按再次生成提交新任务，是本轮修复目标。超时只能说明客户端未收到结果，不能证明上游未生成或未扣费。

第一条没有保存上游任务编号或图片 URL，因此不能直接查询或编造恢复结果。第二条完成记录须保留。开发检查不提交任何新的付费生成请求。

## 修复约束

发出请求后的超时、断网和无法确认的响应进入结果待确认，保留原请求 ID，禁止自动重提。重新生成前必须读取原收据；无法查询或状态未确定时阻止新提交，已完成则先恢复显示。用户明确再次生成时提示这是新的付费任务。

## 验收结果

上述修复已实施，同步等待延长至 300 秒。90 项测试通过，前后端语法检查通过。缓存版本 studio151，后端已重启。HTTP 查询确认第一条为 unknown/submissionUnknown=true，第二条为 completed；没有重新提交生成请求。
