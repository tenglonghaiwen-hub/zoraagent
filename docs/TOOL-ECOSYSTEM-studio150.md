# 工具生态进度

> 维护说明（2026-09-15）：当前能力、入口与限制见 [当前实现状态](CURRENT-STATUS.md)。本文中的版本验收数字、当时状态和后续计划保留为历史记录；涉及 RunningHub 的接入计划已取消，相关接口现已移除。

本轮增加容器内目录列表 `list` 和固定字符串递归文件搜索 `search`，均复用审批、独立工作区、超时和输出限制。任务页显示搜索条件及 stdout/stderr。客户端缓存 studio150。

验证：71 项单元测试通过，审批界面模拟测试通过。未将模拟执行当作真实容器成功。

Docker Desktop 4.90.0 安装成功，CLI 报告 Docker 29.7.2。引擎尚未通过启动验证；旧 WSL 需要升级，首次新版 WSL 安装因需要管理员权限失败，已发起管理员安装。尚未下载运行镜像、未完成真实文件读写测试。

后续仍需实现并独立验收：

- 工具完成后持续唤醒模型并传入执行结果，跨重启恢复时不重复提交。
- MCP 配置、连接、工具目录及权限管理。
- 浏览器会话与操作工具。
- 针对修改前内容校验的代码补丁，以及长时间终端任务控制。
- 媒体完成事件与后续任务之间的数据传递。

这些能力尚未完成，不应宣称已拥有完整 Codex 工具生态。
