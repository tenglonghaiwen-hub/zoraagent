import test from 'node:test';import assert from 'node:assert/strict';import {loadStoredMinimaxKey} from '../apps/server/minimax-credentials.mjs';
test('explicit key wins; encrypted key precedes media fallback',()=>{const env={MINIMAX_API_KEY:'explicit'};assert.equal(loadStoredMinimaxKey('.',{env}),'environment');const second={DUOYUANX_API_KEY:'media'};assert.equal(loadStoredMinimaxKey('.',{env:second,platform:'win32',exists:()=>true,exec:()=> 'stored'}),'encrypted-store');assert.equal(second.MINIMAX_API_KEY,'stored');assert.equal(second.DUOYUANX_API_KEY,'media');});
test('decryption failures do not expose secrets or silently use media key',()=>{const env={DUOYUANX_API_KEY:'media'};assert.throws(()=>loadStoredMinimaxKey('.',{env,platform:'win32',exists:()=>true,exec:()=>{throw Error('sensitive');}}),e=>!e.message.includes('sensitive'));assert.equal(env.MINIMAX_API_KEY,undefined);assert.equal(loadStoredMinimaxKey('.',{env,exists:()=>false}),'fallback');});
