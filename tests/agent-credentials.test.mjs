import test from 'node:test';import assert from 'node:assert/strict';import {loadStoredAgentKey} from '../apps/server/agent-credentials.mjs';
test('explicit key wins; encrypted key precedes media fallback',()=>{const env={ZORA_AGENT_API_KEY:'explicit'};assert.equal(loadStoredAgentKey('.',{env}),'environment');const second={DUOYUANX_API_KEY:'media'};assert.equal(loadStoredAgentKey('.',{env:second,platform:'win32',exists:()=>true,exec:()=> 'stored'}),'encrypted-store');assert.equal(second.ZORA_AGENT_API_KEY,'stored');assert.equal(second.DUOYUANX_API_KEY,'media');});
test('decryption failures do not expose secrets or silently use media key',()=>{const env={DUOYUANX_API_KEY:'media'};assert.throws(()=>loadStoredAgentKey('.',{env,platform:'win32',exists:()=>true,exec:()=>{throw Error('sensitive');}}),e=>!e.message.includes('sensitive'));assert.equal(env.ZORA_AGENT_API_KEY,undefined);assert.equal(loadStoredAgentKey('.',{env,exists:()=>false}),'fallback');});
