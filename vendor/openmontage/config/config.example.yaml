# Copied shape from OpenMontage config.yaml — edit for Zora-hosted engine
llm:
  provider: anthropic
  model: null
  temperature: 0.7
  max_tokens: 4096

budget:
  mode: warn
  total_usd: 10.00

output:
  default_format: mp4
  default_resolution: "1920x1080"
  default_fps: 30

paths:
  pipeline_dir: pipeline
  library_dir: library
  styles_dir: styles
  skills_dir: skills
  output_dir: output
