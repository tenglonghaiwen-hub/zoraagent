{
  "name": "zora-cloudflare-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Zora 官方云端中转网关：基于 Cloudflare Workers + D1，实现 0 云主机成本的商业鉴权、计费扣费与模型安全代理",
  "main": "src/index.mjs",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute zora-db --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
